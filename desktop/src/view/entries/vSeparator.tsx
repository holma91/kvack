import { createRoot } from 'react-dom/client';
import VSeparator from '../components/VSeparator';
const container = document.getElementById('root');
const root = createRoot(container);

root.render(<VSeparator />);
