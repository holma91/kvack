import { createRoot } from 'react-dom/client';
import HSeparator from './components/HSeparator';
const container = document.getElementById('root');
const root = createRoot(container);

root.render(<HSeparator />);
